# Test configuration for Go plugin (.so) type
# Usage:
#   1. go build -buildmode=plugin -o ../plugin-goplugin/goplugin.so ../plugin-goplugin
#   2. cp devgen-goplugin.toml devgen.toml && devgen ./testdata

[[plugins]]
name = "goplugin"
path = "../plugin-goplugin/goplugin.so"
type = "plugin"
